# server.port=8080

# spring.datasource.url=jdbc:postgresql://localhost:5454/gameoflife
# spring.datasource.username=gameuser
# spring.datasource.password=secret
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

server.port=8080

# Database connection - uses Docker service name 'db' and internal port 5432
spring.datasource.url=jdbc:postgresql://db:5432/gameoflife
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD}

# Let Hibernate create tables automatically (dev mode)
spring.jpa.hibernate.ddl-auto=create-drop

# Useful for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
app.ai.model=qwen/qwen3-8b
